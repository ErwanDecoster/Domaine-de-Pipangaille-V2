---
import NotFoundTemplate from "@/components/templates/NotFoundTemplate.astro";
import { getCurrentLang } from "@/lib/i18n";

const lang = getCurrentLang(Astro.currentLocale);
---

<NotFoundTemplate lang={lang} />
